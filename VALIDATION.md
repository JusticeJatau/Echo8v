# Validation

- Production Vite build passed after removing SiteContext and the unused infrastructure.
- Backend JavaScript syntax checks passed.
- Fixed brand assets are referenced directly from `/media`.
- Real MongoDB, Backblaze, and Resend calls require your credentials and must be checked after filling `backend/.env`.
