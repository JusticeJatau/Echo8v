{
  "name": "echo8v",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "npm start -w backend",
    "seed:admin": "npm run seed:admin -w backend",
    "seed:content": "npm run seed:content -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
